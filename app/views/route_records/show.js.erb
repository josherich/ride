var content = $("<%= escape_javascript( render :partial => 'show_overlay', :object => @route_record, :as => :route_record ) %></div>");
var target = $('#route_detail_content');

$('body').addClass('route_detail_overlay_enabled');
target.html(content);