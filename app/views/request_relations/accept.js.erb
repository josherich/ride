alert("gotcha");
var target = $("#<%= @request_relation.id %> .request_action");
var prograss_bar = $('div#route_record_<%= @request_relation.reqed_id %> div.progress > div');
var content = $("<%= escape_javascript(render :partial => 'users/unaccept', :locals => { :reqed_id => @reqed_id, :request_relation => @request_relation }) %>");
target.html(content);