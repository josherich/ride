= link_to(route_path(route), :remote => true) do
	%button.btn.detail-btn
		详细
= link_to requests_route_path(route, :type => 'unrequest'), :remote => true, :method => :post do
	%button.btn.cancel-btn
		取消